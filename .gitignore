# Media (large binary files, not versioned)
media/
output/

# Swift
transcription/.build/
transcription/.swiftpm/
*.xcodeproj

# Python
pipeline/__pycache__/
pipeline/.venv/
*.pyc

# macOS
.DS_Store

# Debug scratch (per-window transcript dumps from manual scoring pass)
pipeline/window_texts/
transcription/dist/

# Credentials -- must never be committed (keys live in Keychain/env only)
*.p8
*.p12
*.pfx
*.pem
*.key
gcp-oauth.keys.json
tokens.json
.env
.env.*
